 <h1>{{ author.username }}</h1>
    <img src="{{ author.picture }}" alt="{{ author.username }}" width="200">
    <p>Email : {{ author.email }}</p>
    <p>Nombre de livres : {{ author.nb_books }}</p>

    <a href="{{ path('list_authors') }}">Retourner à la liste</a>